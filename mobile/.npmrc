# Expo + npm peer-dependency resolution.
# Expo pulls web deps (react-dom, radix, vaul via expo-router) whose peer
# ranges don't perfectly line up with Expo's pinned react. This is safe for
# Expo apps and lets `npm install` work without the --legacy-peer-deps flag.
legacy-peer-deps=true
